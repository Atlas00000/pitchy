/**
 * Ties the hero preview to real product surfaces — not UI chrome copy.
 */

export function HeroTrustLine() {
  return (
    <p className="mt-8 max-w-xl text-left text-sm leading-relaxed text-pitchly-text-muted">
      The preview mirrors what lands after upload:{" "}
      <span className="font-medium text-pitchly-text-secondary">
        four-dimension score bars, objection weighting, and coaching-note density
      </span>{" "}
      in the same layout as the live call report — illustrative values only, no workspace data.
    </p>
  )
}
