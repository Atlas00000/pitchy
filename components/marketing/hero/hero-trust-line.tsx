/**
 * Honest caption — no fake customer logos or vanity metrics.
 */

export function HeroTrustLine() {
  return (
    <p className="mt-8 max-w-xl text-left text-sm leading-relaxed text-pitchly-text-muted">
      Readout below is a{" "}
      <span className="font-medium text-pitchly-text-secondary">static example</span> of the analysis
      layout — same dimensions and score bands as the live product, not a recording of your data.
    </p>
  )
}
